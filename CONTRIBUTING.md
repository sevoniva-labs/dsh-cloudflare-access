# 贡献指南

## 开发流程

1. 创建工作分支，保持修改范围集中。
2. 为行为变更补充测试，执行 `npm run check`。
3. 将对应 `dist/` 构建产物一并提交。
4. 涉及 Cloudflare API、认证或连接生命周期时，按 [测试与验收](docs/verification.md) 完成独立环境验证。

## 隐私与发布

- 示例使用 `example.com`、`example.net` 或测试专用虚构值。
- 不提交真实部署地址、个人邮箱、本机用户名或绝对路径、账号与资源 ID。
- 不提交 Token、API Key、密码、Cookie、JWT、凭据文件、日志、浏览器状态、截图或部署备份。
- 运行数据和本地验证资料放在被 Git 忽略的目录中；提交前检查暂存区和 `npm pack --dry-run` 的文件清单。
- 检查 Git author/committer 元数据；如需保护邮箱，使用 GitHub 提供的 no-reply 邮箱。
- 不在公共文档中记录个人环境的部署过程、账号状态或账单信息。

发现敏感信息已经进入历史记录时，停止发布并联系维护者处理；仅在最新提交中删除文件不足以清除历史内容。
